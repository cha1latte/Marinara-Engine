v1.3.1: mobile layout fixes, regex import, icon updates, DB hardening

Features:
- Bulk import for regex scripts via JSON in Agents panel
- Regex Scripts section moved to top of Agents tab

UI/UX:
- Import icons use downward-arrow; export icons use upward-arrow
- Mobile HUD widgets evenly distributed with proper toolbar spacing
- Echo Chamber dynamically positions below HUD on mobile
- Fixed mobile world state widgets too wide / overlapping action buttons
- Fixed tracker widget touching three-dots menu button
- Fixed desktop widgets constrained to incorrect max-width

Bug Fixes:
- SQLite IOERR_WRITE from macOS iCloud fileproviderd (busy_timeout + .nosync)
- getDB() race condition creating duplicate connections
- busy_timeout PRAGMA missing from better-sqlite3 path
- Regex import placement field type normalization (array vs string)
- CSS @import ordering violation in globals.css
- Removed orphaned .rpg-hud-actions CSS selector
- Cleaned up inline styles and stale comments

Docs:
- Updated README with v1.3.1 changelog and improved update instructions
